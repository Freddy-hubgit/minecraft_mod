{
  "schemaVersion": 1,
  "id": "glitch-death",
  "version": "${version}",
  "name": "Glitch Death",
  "description": "Lets the server choose normal-name, glitch, or random-name replacements for invisible killers while preserving the vanilla death message.",
  "authors": ["Freddy"],
  "license": "MIT",
  "environment": "server",
  "mixins": [
    "glitch-death.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.19.5",
    "fabric-api": ">=0.160.0",
    "minecraft": "~26.2",
    "java": ">=25"
  }
}
